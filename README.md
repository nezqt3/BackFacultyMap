# Backend - карта факультета

## 📘 Описание
BackFacultyMap — backend-сервис для управления картой факультетов (кафедр), аудиторий, возможно — расписаний / коридоров / помещений / пр.  
Цель: предоставить API для CRUD операций над сущностями «faculty / department», «room / classroom», «building» и т.п., с возможностью разворачивания через Docker.

## 🚀 Технологический стек

| Слой / Компонент      | Технологии / Инструменты                               |
|-----------------------|---------------------------------------------------------|
| Ядро приложения       | Java (11+ или 17+), Maven                               |
| Веб / HTTP API        | Spring Boot (REST API)                                  |
| Контейнеризация       | Docker (Dockerfile)                                     |
| Сборка / Зависимости  | Maven (pom.xml)                                         |

> ℹ️ **Примечание:** В README репозитория напрямую не прописано описание, поэтому стек и детали API придётся уточнить, исходя из кода.

## 📁 Структура репозитория  
```
├─ src/main/ — исходники Java
├─ pom.xml — конфигурация Maven
├─ Dockerfile — для сборки Docker-образа
├─ README.md — (этот файл)
├─ .gitignore
└─ .idea/ — IDE-конфиги
```

## 🧰 Установка и запуск

```bash
# 1. Склонировать репозиторий
git clone https://github.com/nezqt3/BackFacultyMap.git
cd BackFacultyMap

# 2. Сборка через Maven
mvn clean install

# 3. Запуск приложения
# (либо напрямую через Java, либо через Docker)
docker build -t backfacultymap .
docker run -p 8080:8080 backfacultymap
```

## REST-эндпоинты

| Метод | Эндпоинт                      | Описание                          | Аргументы запроса |
|-------|-------------------------------|-----------------------------------|------------------|
| GET    | `/api/route`                  | Получить путь от разных кабинетов | `from` (string, обязательно) — начальная точка<br>`to` (string, обязательно) — конечная точка<br>`study_place` (string, обязательно) — корпус/место |
| GET    | `/`                           | Получить полные данные о корпусе  | — |
| GET    | `/api/get_auditoriums/floors` | Получить все кабинеты этажа       | `floor` (int, обязательно) — номер этажа<br>`study_place` (string, обязательно) — корпус/место |
| GET    | `/api/get_auditoriums/frames` | Получить все кабинеты корпусов    | `frame` (int, обязательно) — номер корпуса<br>`study_place` (string, обязательно) — корпус/место |